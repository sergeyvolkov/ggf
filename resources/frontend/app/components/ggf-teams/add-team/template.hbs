{{#if search}}

  {{select-2
    value=team
    minimumInputLength=3
    multiple=false
    query="queryOptions"
    placeholder="Choose team"
  }}

  {{md-btn
    text='Undo'
    action='undo'
  }}

{{else}}
  {{title}}
{{/if}}

{{#if search}}
  {{md-btn
    text='Add team'
    action='submit'
  }}
{{else}}
  {{md-btn
    text='Add team'
    action='search'
  }}
{{/if}}