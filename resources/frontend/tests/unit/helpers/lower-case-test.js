import { lowerCase } from '../../../helpers/lower-case';
import { module, test } from 'qunit';

module('Unit | Helper | lower case');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = lowerCase(42);
  assert.ok(result);
});
