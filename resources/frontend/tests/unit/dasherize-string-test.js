import { dasherizeString } from '../../../helpers/dasherize-string';
import { module, test } from 'qunit';

module('Unit | Helper | dasherize string');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dasherizeString(42);
  assert.ok(result);
});
